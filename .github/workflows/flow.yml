name: "Test env"
on:
  workflow_dispatch

jobs:
  github:
    timeout-minutes: 30
    runs-on: ubuntu-latest

    steps:
      - name: "Echo ENV"
        run : "echo ${{ env.GIT_HUB_API_TOKEN }}"