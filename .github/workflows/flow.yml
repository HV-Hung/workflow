name: "Test env"
on:
  - workflow_dispatch
  - push

jobs:
  github:
    timeout-minutes: 30
    runs-on: ubuntu-latest

    steps:
      - name: "Echo ENV"
        run : "echo ${{ vars.TEST_VAR }}"